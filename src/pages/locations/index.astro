---
import Base from '../../layouts/Base.astro';
import CTA from '../../components/CTA.astro';
import { BUSINESS, url } from '../../config';
import { LOCATIONS } from '../../data/locations';
---

<Base
  title={`Service Area: ${LOCATIONS.length} Twin Cities Metro Cities | ${BUSINESS.name}`}
  description={`JSM provides commercial cleaning in ${LOCATIONS.length} metro cities including ${LOCATIONS.slice(0, 4).map((l) => l.name).join(', ')}. Free quotes within 24 hours.`}
>
  <section class="section">
    <div class="container">
      <nav class="crumbs"><a href={url('/')}>Home</a> / Locations</nav>
      <div class="section-head">
        <h1>Our Service Area</h1>
        <p>
          Local crews positioned across the {BUSINESS.serviceArea} for fast response and
          consistent recurring service. Don't see your city? Call us — we likely cover it.
        </p>
      </div>
      <div class="grid grid-3">
        {LOCATIONS.map((l) => (
          <div class="card">
            <h3><a href={url(`/locations/${l.slug}`)}>{l.name}, MN</a></h3>
            <p>{l.blurb}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
  <CTA />
</Base>
